# Artwork Extractor

## CLI

```
usage: artwork-dl [-h] [--file FILE] url

Download artwork from music services

positional arguments:
  url                   album/song URL

options:
  -h, --help            show this help message and exit
  --file FILE, -f FILE  mutiple albums/songs URLs as a fil

```

## UI
